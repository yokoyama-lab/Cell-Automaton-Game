{
  "name": "cell-automaton-game-web",
  "version": "1.0.0",
  "private": true,
  "description": "Cell-Automaton-Game の TypeScript + Canvas 版（ブラウザで動作）",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
